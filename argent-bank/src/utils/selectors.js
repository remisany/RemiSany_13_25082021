export const selectToken = (state) => state.logIn.token

export const selectFirstname = (state) => state.profile.firstname

export const selectLastname = (state) => state.profile.lastname

export const selectChecked = (state) => state.signIn.remember

export const selectEmail = (state) => state.signIn.email

export const selectPassword = (state) => state.signIn.password

